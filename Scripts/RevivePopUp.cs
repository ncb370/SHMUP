using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class RevivePopUp : MonoBehaviour
{

    //[SerializeField] public Text reviveMsg;
    //private float rTimer;
    //private Color rColor;
    //float r = 0.09811318f, g = 1.0f, b = 0.1212814f, a = 0.0f;


    // Start is called before the first frame update
    void Start()
    {
        //reviveMsg.gameObject.GetComponent<ReviveText>().a = 0;
        //reviveMsg.color = new Color(r, g, b, a);
    }

    // Update is called once per frame
    void Update()
    {
         //if (a > 0.0f)
        //{
          //  a -= 0.1f;
        //}
    }

   
}
